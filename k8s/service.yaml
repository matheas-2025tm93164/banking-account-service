apiVersion: v1
kind: Service
metadata:
  name: account-service
  namespace: banking-system
  labels:
    app: account-service
spec:
  type: NodePort
  selector:
    app: account-service
  ports:
    - name: http
      port: 8002
      targetPort: 8002
